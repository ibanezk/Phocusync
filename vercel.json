{
  "buildCommand": "cd app && npm install && npm run build",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/app/api/$1"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/app/dist/index.html"
    }
  ]
}
